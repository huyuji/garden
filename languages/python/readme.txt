run filter.py, run "filter.py -h" to see usage information
adjust "high", "low" and "filter type" as needed
click "process" button
the processed image and the filter will be displayed. It may take a little long if gaussian filter is selected
click "save" button to save the processed image and the filter in the current folder